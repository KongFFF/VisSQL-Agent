
## 微调

### v1（lora）
#### 参数
bf16: true
cutoff_len: 2048
dataset: spider_sql
dataset_dir: data
ddp_timeout: 180000000
do_train: true
enable_thinking: true
finetuning_type: lora
flash_attn: auto
gradient_accumulation_steps: 16
include_num_input_tokens_seen: true
learning_rate: 5.0e-05
logging_steps: 5
lora_alpha: 16
lora_dropout: 0
lora_rank: 8
lora_target: all
lr_scheduler_type: cosine
max_grad_norm: 1.0
max_samples: 100000
model_name_or_path: /root/autodl-tmp/qwen2.5-coder-7b-instruct
num_train_epochs: 3.0
optim: adamw_torch
output_dir: saves/Qwen2.5-7B/lora/qwen_spider_lora_v1
packing: false
per_device_train_batch_size: 1
plot_loss: true
preprocessing_num_workers: 16
report_to: none
save_steps: 100
stage: sft
template: qwen
trust_remote_code: true
warmup_steps: 0

#### 数据格式

`main_eval.py`
```python
def build_spider_schemas(tables_json_path: str) -> dict:
    """把 Spider 极其反人类的 JSON 格式，解析成大模型最爱看的 CREATE TABLE 字符串字典。"""
    with open(tables_json_path, 'r', encoding='utf-8') as f:
        tables_data = json.load(f)
        
    schemas = {}
    for db in tables_data:
        db_id = db['db_id']
        table_names = db['table_names_original']
        col_names = db['column_names_original']
        tables = {i: [] for i in range(len(table_names))}
        for col in col_names:
            table_idx, col_name = col[0], col[1]
            if table_idx >= 0:
                tables[table_idx].append(col_name)
        schema_str = ""
        for i, t_name in enumerate(table_names):
            cols = ", ".join(tables[i])
            schema_str += f"CREATE TABLE {t_name} ({cols});\n"
        schemas[db_id] = schema_str.strip()
    return schemas
```

`llm_inference.py`
```python
def generate_sql(self, question: str, table_schema: str) -> str:
        """
        核心推理逻辑：接收问题和表结构，生成并返回 SQL 字符串。
        """

        # 构造 Qwen 专属的 Prompt 模板（把 question 和 table_schema 塞进去）
        messages = [
           {"role": "system", "content": "你是一个优秀的数据库专家。请根据提供的表结构，直接输出标准的 SQLite 查询语句，不要包含任何额外的解释。"},
            {"role": "user", "content": f"【表结构】\n{table_schema}\n\n【问题】\n{question}"}
        ]

        # 1. 纯净模板拼接：只拼成带 <|im_start|> 的字符串，不做 Tensor 转化
        prompt_text = self.tokenizer.apply_chat_template(
            messages, 
            tokenize=False, # 极其关键：设为 False，只输出字符串
            add_generation_prompt=True 
        )
		
		......
```

```json
"conversations": [
      {
        "from": "system",
        "value": "你是一个顶尖的数据库架构师和SQL专家。请根据以下数据库的表结构，将用户的自然语言问题转化为精确的SQL查询语句。不要输出任何解释性的废话。\n\n【表结构】\n数据库名称: department_management\n表 department 包含列: Department_ID, Name, Creation, Ranking, Budget_in_Billions, Num_Employees\n表 head 包含列: head_ID, name, born_state, age\n表 management 包含列: department_ID, head_ID, temporary_acting"
      },
      {
        "from": "user",
        "value": "How many heads of the departments are older than 56 ?"
      },
      {
        "from": "assistant",
        "value": "SELECT count(*) FROM head WHERE age  >  56"
      }
    ]
  },
  ......
```

#### 结果
71%

### v2（dora）

`main_eval.py`
```python
def build_spider_schemas(tables_json_path: str) -> dict:
    """把 Spider 极其反人类的 JSON 格式，解析成大模型最爱看的 CREATE TABLE 字符串字典。"""
    with open(tables_json_path, 'r', encoding='utf-8') as f:
        tables_data = json.load(f)
        
    schemas = {}
    for db in tables_data:
        db_id = db['db_id']
        table_names = db['table_names_original']
        col_names = db['column_names_original']
        tables = {i: [] for i in range(len(table_names))}
        for col in col_names:
            table_idx, col_name = col[0], col[1]
            if table_idx >= 0:
                tables[table_idx].append(col_name)
        schema_str = ""
        for i, t_name in enumerate(table_names):
            cols = ", ".join(tables[i])
            schema_str += f"CREATE TABLE {t_name} ({cols});\n"
        schemas[db_id] = schema_str.strip()
    return schemas
```

`llm_inference.py`
```python
def generate_sql(self, question: str, table_schema: str) -> str:
        """
        核心推理逻辑：接收问题和表结构，生成并返回 SQL 字符串。
        """

        # 构造 Qwen 专属的 Prompt 模板（把 question 和 table_schema 塞进去）
        messages = [
           {"role": "system", "content": "你是一个优秀的数据库专家。请根据提供的表结构，直接输出标准的 SQLite 查询语句，不要包含任何额外的解释。"},
            {"role": "user", "content": f"【表结构】\n{table_schema}\n\n【问题】\n{question}"}
        ]

        # 1. 纯净模板拼接：只拼成带 <|im_start|> 的字符串，不做 Tensor 转化
        prompt_text = self.tokenizer.apply_chat_template(
            messages, 
            tokenize=False, # 极其关键：设为 False，只输出字符串
            add_generation_prompt=True 
        )
		
		......
```

```json
"conversations": [
      {
        "from": "system",
        "value": "你是一个顶尖的数据库架构师和SQL专家。请根据以下数据库的表结构，将用户的自然语言问题转化为精确的SQL查询语句。不要输出任何解释性的废话。\n\n【表结构】\n数据库名称: department_management\n表 department 包含列: Department_ID, Name, Creation, Ranking, Budget_in_Billions, Num_Employees\n表 head 包含列: head_ID, name, born_state, age\n表 management 包含列: department_ID, head_ID, temporary_acting"
      },
      {
        "from": "user",
        "value": "How many heads of the departments are older than 56 ?"
      },
      {
        "from": "assistant",
        "value": "SELECT count(*) FROM head WHERE age  >  56"
      }
    ]
  },
```

#### 结果

70.2%

### v3(lora)

#### 参数
bf16: true
cutoff_len: 4096
dataset: spider_golden
dataset_dir: data
ddp_timeout: 180000000
do_train: true
enable_thinking: true
finetuning_type: lora
flash_attn: auto
gradient_accumulation_steps: 16
include_num_input_tokens_seen: true
learning_rate: 5.0e-05
logging_steps: 5
lora_alpha: 16
lora_dropout: 0
lora_rank: 8
lora_target: all
lr_scheduler_type: cosine
max_grad_norm: 1.0
max_samples: 100000
model_name_or_path: /root/autodl-tmp/qwen2.5-coder-7b-instruct
num_train_epochs: 3.0
optim: adamw_torch
output_dir: saves/Qwen2.5-7B/lora/qwen_spider_lora_v3
packing: false
per_device_train_batch_size: 1
plot_loss: true
preprocessing_num_workers: 16
report_to: none
save_steps: 100
stage: sft
template: qwen
trust_remote_code: true
warmup_steps: 0


#### 数据格式

`main_eval.py`
```python
def build_golden_ddl(tables_path):
    """深度挖掘 Spider 的 tables.json，提取类型、主键、外键，生成纯正的 DDL 语句。"""
    with open(tables_path, 'r', encoding='utf-8') as f:
        tables_data = json.load(f)
    db_schemas = {}
    for db in tables_data:
        db_id = db['db_id']
        table_names = db['table_names_original']
        col_names = db['column_names_original']  
        col_types = db['column_types']            
        primary_keys = db['primary_keys']        
        foreign_keys = db['foreign_keys']        
        
        tables_dict = {i: {"name": t_name, "cols": [], "pks": [], "fks": []} for i, t_name in enumerate(table_names)}
        
        for col_idx, (table_idx, col_name) in enumerate(col_names):
            if table_idx == -1: continue
            c_type = col_types[col_idx].upper()
            if c_type == 'NUMBER': c_type = 'INT'
            elif c_type == 'TEXT': c_type = 'VARCHAR'
            elif c_type == 'TIME': c_type = 'DATETIME'
            elif c_type == 'BOOLEAN': c_type = 'BOOLEAN'
            elif c_type == 'OTHERS': c_type = 'VARCHAR' 
            tables_dict[table_idx]["cols"].append(f"{col_name} {c_type}")
            if col_idx in primary_keys:
                tables_dict[table_idx]["pks"].append(col_name)
                
        for fk in foreign_keys:
            src_col_idx, tgt_col_idx = fk
            src_table_idx, src_col_name = col_names[src_col_idx][0], col_names[src_col_idx][1]
            tgt_table_idx, tgt_col_name = col_names[tgt_col_idx][0], col_names[tgt_col_idx][1]
            tgt_table_name = table_names[tgt_table_idx]
            fk_str = f"FOREIGN KEY ({src_col_name}) REFERENCES {tgt_table_name}({tgt_col_name})"
            tables_dict[src_table_idx]["fks"].append(fk_str)
            
        ddl_lines = [f"-- 数据库: {db_id}"]
        for t_idx, t_info in tables_dict.items():
            t_name = t_info["name"]
            table_body_lines = []
            table_body_lines.extend([f"  {c}" for c in t_info["cols"]])
            if t_info["pks"]:
                pks_str = ", ".join(t_info["pks"])
                table_body_lines.append(f"  PRIMARY KEY ({pks_str})")
            table_body_lines.extend([f"  {fk}" for fk in t_info["fks"]])
            table_body = ",\n".join(table_body_lines)
            ddl = f"CREATE TABLE {t_name} (\n{table_body}\n);"
            ddl_lines.append(ddl)
        db_schemas[db_id] = "\n\n".join(ddl_lines)
    return db_schemas
```

`llm_inference.py`
```python
def generate_sql(self, question: str, table_schema: str) -> str:
        """
        核心推理逻辑：接收问题和表结构，生成并返回 SQL 字符串。
        """
# # 【核心修复：系统提示词必须与黄金训练集一字不差！】
        system_prompt = (
             "你是一个极其专业的数据库架构师。请仔细阅读以下包含数据类型、主键(PRIMARY KEY)和外键(FOREIGN KEY)的数据库 DDL 结构。\n"
             "严格遵循表间关联关系，将用户的自然语言问题转化为极其精确的 SQL 查询语句。不要输出任何解释性文本。\n\n"
             f"{table_schema}"
         )
         
         
         # 严格对齐黄金数据集的角色分配：表结构放 System，问题放 User
         messages = [
             {"role": "system", "content": system_prompt},
             {"role": "user", "content": question}
         ]
		
		......
```

```json
{
    "system": "你是一个极其专业的数据库架构师。请仔细阅读以下包含数据类型、主键(PRIMARY KEY)和外键(FOREIGN KEY)的数据库 DDL 结构。\n严格遵循表间关联关系，将用户的自然语言问题转化为极其精确的 SQL 查询语句。不要输出任何解释性文本。\n\n-- 数据库: department_management\n\nCREATE TABLE department (\n  Department_ID INT,\n  Name VARCHAR,\n  Creation VARCHAR,\n  Ranking INT,\n  Budget_in_Billions INT,\n  Num_Employees INT,\n  PRIMARY KEY (Department_ID)\n);\n\nCREATE TABLE head (\n  head_ID INT,\n  name VARCHAR,\n  born_state VARCHAR,\n  age INT,\n  PRIMARY KEY (head_ID)\n);\n\nCREATE TABLE management (\n  department_ID INT,\n  head_ID INT,\n  temporary_acting VARCHAR,\n  PRIMARY KEY (department_ID),\n  [11, 1],\n  [11, 1]\n);",
    "conversations": [
      {
        "from": "user",
        "value": "How many heads of the departments are older than 56 ?"
      },
      {
        "from": "assistant",
        "value": "SELECT count(*) FROM head WHERE age  >  56"
      }
    ]
  },
  ......
```

#### 结果

70.8%

### v4(dora)

#### 参数
bf16: true
cutoff_len: 4096
dataset: spider_golden
dataset_dir: data
ddp_timeout: 180000000
do_train: true
enable_thinking: true
finetuning_type: lora
flash_attn: auto
gradient_accumulation_steps: 16
include_num_input_tokens_seen: true
learning_rate: 5.0e-05
logging_steps: 5
lora_alpha: 64
lora_dropout: 0
lora_rank: 32
lora_target: all
lr_scheduler_type: cosine
max_grad_norm: 1.0
max_samples: 100000
model_name_or_path: /root/autodl-tmp/qwen2.5-coder-7b-instruct
num_train_epochs: 3.0
optim: adamw_torch
output_dir: saves/Qwen2.5-7B/lora/qwen_spider_lora_v4
packing: false
per_device_train_batch_size: 1
plot_loss: true
preprocessing_num_workers: 16
report_to: none
resume_from_checkpoint: true
save_steps: 100
stage: sft
template: qwen
trust_remote_code: true
use_dora: true
warmup_steps: 0


#### 数据格式

`main_eval.py`
```python
def build_golden_ddl(tables_path):
    """深度挖掘 Spider 的 tables.json，提取类型、主键、外键，生成纯正的 DDL 语句。"""
    with open(tables_path, 'r', encoding='utf-8') as f:
        tables_data = json.load(f)
    db_schemas = {}
    for db in tables_data:
        db_id = db['db_id']
        table_names = db['table_names_original']
        col_names = db['column_names_original']  
        col_types = db['column_types']            
        primary_keys = db['primary_keys']        
        foreign_keys = db['foreign_keys']        
        
        tables_dict = {i: {"name": t_name, "cols": [], "pks": [], "fks": []} for i, t_name in enumerate(table_names)}
        
        for col_idx, (table_idx, col_name) in enumerate(col_names):
            if table_idx == -1: continue
            c_type = col_types[col_idx].upper()
            if c_type == 'NUMBER': c_type = 'INT'
            elif c_type == 'TEXT': c_type = 'VARCHAR'
            elif c_type == 'TIME': c_type = 'DATETIME'
            elif c_type == 'BOOLEAN': c_type = 'BOOLEAN'
            elif c_type == 'OTHERS': c_type = 'VARCHAR' 
            tables_dict[table_idx]["cols"].append(f"{col_name} {c_type}")
            if col_idx in primary_keys:
                tables_dict[table_idx]["pks"].append(col_name)
                
        for fk in foreign_keys:
            src_col_idx, tgt_col_idx = fk
            src_table_idx, src_col_name = col_names[src_col_idx][0], col_names[src_col_idx][1]
            tgt_table_idx, tgt_col_name = col_names[tgt_col_idx][0], col_names[tgt_col_idx][1]
            tgt_table_name = table_names[tgt_table_idx]
            fk_str = f"FOREIGN KEY ({src_col_name}) REFERENCES {tgt_table_name}({tgt_col_name})"
            tables_dict[src_table_idx]["fks"].append(fk_str)
            
        ddl_lines = [f"-- 数据库: {db_id}"]
        for t_idx, t_info in tables_dict.items():
            t_name = t_info["name"]
            table_body_lines = []
            table_body_lines.extend([f"  {c}" for c in t_info["cols"]])
            if t_info["pks"]:
                pks_str = ", ".join(t_info["pks"])
                table_body_lines.append(f"  PRIMARY KEY ({pks_str})")
            table_body_lines.extend([f"  {fk}" for fk in t_info["fks"]])
            table_body = ",\n".join(table_body_lines)
            ddl = f"CREATE TABLE {t_name} (\n{table_body}\n);"
            ddl_lines.append(ddl)
        db_schemas[db_id] = "\n\n".join(ddl_lines)
    return db_schemas
```

`llm_inference.py`
```python
def generate_sql(self, question: str, table_schema: str) -> str:
        """
        核心推理逻辑：接收问题和表结构，生成并返回 SQL 字符串。
        """
# # 【核心修复：系统提示词必须与黄金训练集一字不差！】
        system_prompt = (
             "你是一个极其专业的数据库架构师。请仔细阅读以下包含数据类型、主键(PRIMARY KEY)和外键(FOREIGN KEY)的数据库 DDL 结构。\n"
             "严格遵循表间关联关系，将用户的自然语言问题转化为极其精确的 SQL 查询语句。不要输出任何解释性文本。\n\n"
             f"{table_schema}"
         )
         
         
         # 严格对齐黄金数据集的角色分配：表结构放 System，问题放 User
         messages = [
             {"role": "system", "content": system_prompt},
             {"role": "user", "content": question}
         ]
		
		......
```

```json
{
    "system": "你是一个极其专业的数据库架构师。请仔细阅读以下包含数据类型、主键(PRIMARY KEY)和外键(FOREIGN KEY)的数据库 DDL 结构。\n严格遵循表间关联关系，将用户的自然语言问题转化为极其精确的 SQL 查询语句。不要输出任何解释性文本。\n\n-- 数据库: department_management\n\nCREATE TABLE department (\n  Department_ID INT,\n  Name VARCHAR,\n  Creation VARCHAR,\n  Ranking INT,\n  Budget_in_Billions INT,\n  Num_Employees INT,\n  PRIMARY KEY (Department_ID)\n);\n\nCREATE TABLE head (\n  head_ID INT,\n  name VARCHAR,\n  born_state VARCHAR,\n  age INT,\n  PRIMARY KEY (head_ID)\n);\n\nCREATE TABLE management (\n  department_ID INT,\n  head_ID INT,\n  temporary_acting VARCHAR,\n  PRIMARY KEY (department_ID),\n  [11, 1],\n  [11, 1]\n);",
    "conversations": [
      {
        "from": "user",
        "value": "How many heads of the departments are older than 56 ?"
      },
      {
        "from": "assistant",
        "value": "SELECT count(*) FROM head WHERE age  >  56"
      }
    ]
  },
  ......
```

#### 结果
70%


### v5(lora)

#### 参数
bf16: true
cutoff_len: 3072
dataset: spider_v5
dataset_dir: data
ddp_timeout: 180000000
do_train: true
enable_thinking: true
finetuning_type: lora
flash_attn: auto
gradient_accumulation_steps: 16
include_num_input_tokens_seen: true
learning_rate: 2.5e-05
logging_steps: 5
lora_alpha: 128
lora_dropout: 0
lora_rank: 64
lora_target: all
lr_scheduler_type: cosine
max_grad_norm: 1.0
max_samples: 100000
model_name_or_path: /root/autodl-tmp/qwen2.5-coder-7b-instruct
num_train_epochs: 3.0
optim: adamw_torch
output_dir: saves/Qwen2.5-7B/lora/qwen_spider_lora_v5
packing: false
per_device_train_batch_size: 1
plot_loss: true
preprocessing_num_workers: 16
report_to: none
save_steps: 100
stage: sft
template: qwen
trust_remote_code: true
warmup_steps: 50

#### 数据格式

`main_eval.py`
```python
def build_schema_dict_v5(tables_path):
    """提取半结构化（表 + 列 + PK + FK）降噪格式。"""
    with open(tables_path, 'r', encoding='utf-8') as f:
        tables_data = json.load(f)
    db_schemas = {}
    for db in tables_data:
        db_id = db['db_id']
        table_names = db['table_names_original']
        column_names = db['column_names_original']
        primary_keys = db['primary_keys']
        foreign_keys = db['foreign_keys']

        tables_dict = {i: [] for i in range(len(table_names))}
        for idx, col in enumerate(column_names):
            table_idx = col[0]
            col_name = col[1]
            if table_idx == -1: continue
            tables_dict[table_idx].append((idx, col_name))

        pk_set = set(primary_keys)
        fk_map = {src: tgt for src, tgt in foreign_keys}

        schema_lines = [f"Database: {db_id}"]
        for i, t_name in enumerate(table_names):
            schema_lines.append(f"\nTable: {t_name}")
            schema_lines.append("Columns:")
            for col_idx, col_name in tables_dict[i]:
                col_desc = f"- {col_name}"
                if col_idx in pk_set:
                    col_desc += " [PK]"
                if col_idx in fk_map:
                    ref_idx = fk_map[col_idx]
                    ref_table = table_names[column_names[ref_idx][0]]
                    ref_col = column_names[ref_idx][1]
                    col_desc += f" [FK -> {ref_table}.{ref_col}]"
                schema_lines.append(col_desc)
        db_schemas[db_id] = "\n".join(schema_lines)
    return db_schemas
```

`llm_inference.py`
```python
def generate_sql(self, question: str, table_schema: str) -> str:
        """
        核心推理逻辑：接收问题和表结构，生成并返回 SQL 字符串。
        """

        # ======= v5适配版本代码 =======
        system_prompt = "Generate SQL based on the database schema. Output only SQL."
        
        user_prompt = f"""Database schema:
{table_schema}

Question:
{question}"""

        messages = [
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_prompt}
        ]
        # ===============================
		......
```

```json
"conversations": [
      {
        "from": "system",
        "value": "Generate SQL based on the database schema. Output only SQL."
      },
      {
        "from": "user",
        "value": "Database schema:\nDatabase: department_management\n\nTable: department\nColumns:\n- Department_ID [PK]\n- Name\n- Creation\n- Ranking\n- Budget_in_Billions\n- Num_Employees\n\nTable: head\nColumns:\n- head_ID [PK]\n- name\n- born_state\n- age\n\nTable: management\nColumns:\n- department_ID [PK] [FK -> department.Department_ID]\n- head_ID [FK -> head.head_ID]\n- temporary_acting\n\nQuestion:\nHow many heads of the departments are older than 56 ?"
      },
      {
        "from": "assistant",
        "value": "SELECT count(*) FROM head WHERE age  >  56"
      }
    ]
  },
  ......
```

#### 结果

72%          

### v6(lora)

alora_invocation_tokensnull
alpha_pattern
arrow_confignull
auto_mappingnull
base_model_name_or_path"/root/autodl-tmp/qwen2.5-coder-7b-instruct"
bias"none"
corda_confignull
ensure_weight_tyingfalse
eva_confignull
exclude_modulesnull
fan_in_fan_outfalse
inference_modetrue
init_lora_weightstrue
layer_replicationnull
layers_patternnull
layers_to_transformnull
loftq_config
lora_alpha128
lora_biasfalse
lora_dropout0
megatron_confignull
megatron_core"megatron.core"
modules_to_savenull
peft_type"LORA"
peft_version"0.18.1"
qalora_group_size16
r64
rank_pattern
revisionnull
0"o_proj"
1"up_proj"
2"k_proj"
3"gate_proj"
4"q_proj"
5"v_proj"
6"down_proj"
target_parametersnull
task_type"CAUSAL_LM"
trainable_token_indicesnull
use_dorafalse
use_qalorafalse
use_rslorafalse

#### 数据格式

当前项目代码就是v6版的数据格式


#### 结果

73.9%
### 最终选择v6版微调模型作为微调结果



## ReACT-Reflexion循环

### 基座模型

- 模型：qwen-2.5-7b-coder-instruct

- 微调方法：lora

- 微调结果：74%准确率

### 大纲规划

#### 1. 🧠 `agent_coder.py`  —— **主厨**

- **角色定位：** 只负责“思考和写sql语句”。
    
- **输入：** 记忆流 Message 列表。
    
- **输出：** SQL 语句。
    
- **特点：** 它没有记忆，你给它什么上下文，它就生成什么。
    

#### 2. 🔨 `agent_executor.py` **(沙盒，语句执行者)**

- **角色定位：** 只负责“执行和挑刺”。
    
- **输入：** SQL 语句。
    
- **输出：** 成功的数据 / 精准的报错字符串。
    

#### 3. 📓 `agent_memory.py` **记事本**

- **角色定位：** 数据结构维护者。它不是用来“联系”主厨和试吃员的，它是用来**记录**他们之间发生了什么的。
    
- **核心作用：** * **维护 Working Memory（工作记忆）：** 保存 `[问题 -> SQL -> 报错 -> 修正 SQL]` 的完整多轮对话树。
    
    - **格式化反馈（The Translator）：** 试吃员（沙盒）给出的报错往往是极其生硬的机器码（比如 `sqlite3.OperationalError: no such column`）。`agent_memory.py` 负责把这句冰冷的代码，包装成大模型最容易听懂的“指导意见”，再写进记事本里。

	- 内存压缩
		为什么我们需要专门写一个 `agent_memory.py`？
		
		直接在 `main_agent.py` 里 `list.append()` 不就行了吗？”
		
		**绝对不行！** 在极其复杂的多轮纠错中，大模型的上下文窗口非常宝贵。如果沙盒抛出了一个长达 200 行的 Traceback 乱码堆栈，直接 `append` 进去，大模型瞬间就会被垃圾信息淹没，甚至忘记原本的问题。

        

#### 4. 🔄 `main_agent.py`  —— ** 状态机/控制流**

- **角色定位：** 系统的**绝对核心**。
    
- **职责：** 负责写 `while` 循环，控制流转逻辑。
    

---

### 🔄 核心流转逻辑（大堂经理视角的 Workflow）

当你运行 `main_agent.py` 提问时，代码底层的真实调用链是这样的（极其硬核的 Reflexion 反思环）：

1. **用户提问：** “查询纽约的员工”
    
2. **主控制流调用【记事本 Memory】：** 记录下用户的原始问题。
    
3. **主控制流调用【主厨 Coder】：** 把记事本扔给主厨，主厨写出初代代码：`SQL_v1`。
    
4. **主控制流调用【记事本 Memory】：** 记录主厨写了 `SQL_v1`。
    
5. **主控制流调用【试吃员 Executor】：** 拿着 `SQL_v1` 去跑。结果试吃员大喊：`报错！找不到 city_name 列！`
    
6. **主控制流调用【记事本 Memory】：** 极其关键！把这个报错翻译成提示词，写进记事本：“你的 SQL_v1 报错了，错误是...请重写。”
    
7. **触发 Reflexion 循环：** 经理发现没跑通，于是带着更新后的记事本，**再次调用【主厨 Coder】**。
    
8. 主厨看着记事本里的报错，深刻反思，写出 `SQL_v2`。
    
9. ......循环直至试吃员说“跑通了”，或者达到了最大重试次数（比如 3 次）。
    

---


### `agent_coder.py`(主厨)

```python
import re
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer
from peft import PeftModel

class CoderNode:
    def __init__(self, base_model_path: str, lora_path: str):
        """
        初始化大模型推理引擎 (动态挂载模式)
        """
        print(f">>> 🧠 正在唤醒基座模型: {base_model_path} ...")
        self.tokenizer = AutoTokenizer.from_pretrained(base_model_path, trust_remote_code=True)

        # 1. 先把笨重的“游戏本体”加载进显存
        base_model = AutoModelForCausalLM.from_pretrained(
            base_model_path,
            device_map="auto",
            torch_dtype=torch.bfloat16,
            trust_remote_code=True
        )

        # 2. 核心魔法：给基座模型穿上 V6 的“外骨骼装甲”
        print(f">>> 🛡️ 正在挂载 V6 LoRA 权重: {lora_path} ...")
        self.model = PeftModel.from_pretrained(base_model, lora_path).eval()
        # 极其强硬的 System Prompt，确立 Agent 的身份和输出规范
        self.system_prompt = (
            "你是一个极其严谨的顶级 SQL 架构师。你的任务是根据提供的数据库结构和自然语言问题，"
            "编写出极其准确、可以直接在 SQLite 中执行的 SQL 语句。\n"
            "【严格指令】：\n"
            "1. 你只能输出 SQL 代码，绝对不要输出任何自然语言解释、客套话或前置/后置说明。\n"
            "2. 如果遇到执行报错反馈，请深刻反思并修正之前的 SQL。\n"
            "3. 只输出最终修正后的 SQL，不要保留旧版本的代码。"
        )

    def _extract_sql(self, text: str) -> str:
        """
        防御性编程：从模型的啰嗦输出中，极其精准地扒出 SQL 代码块。
        """
        # 尝试匹配 ```sql ... ``` 或者 ``` ... ```
        pattern = r"sql)?\s*(.*?)\s*```"
        match = re.search(pattern, text, re.DOTALL | re.IGNORECASE)
        if match:
            return match.group(1).strip()
        # 如果模型很乖没有加代码块，直接返回清理两端空格的原文
        return text.strip()

    def generate(self, memory_messages: list) -> str:
        """
        核心推理节点：接受结构化记忆，返回思考后的 SQL。
        :param memory_messages: 纯净的 JSON 对话列表，例如 [{"role": "user", "content": "..."}]
        """
		
        # 1. 注入系统级护栏 (System Prompt)
        # 我们在每次对话的最开头，强行塞入 System 设定
        full_messages = [{"role": "system", "content": self.system_prompt}] + memory_messages

        # 2. 核心魔法：使用 apply_chat_template 自动组装 ChatML 格式！
        # 底层框架会根据 Qwen 的专属格式，全自动帮你加上 <|im_start|> 等特殊 Token。
        text = self.tokenizer.apply_chat_template(
            full_messages,
            tokenize=False,
            add_generation_prompt=True # 告诉模型：现在轮到 assistant 说话了
        )
        model_inputs = self.tokenizer([text], return_tensors="pt").to(self.model.device)

        # 3. 生成配置 (彻底抹杀随机性)
        # 因为我们写的是严谨的 SQL Agent，不是写诗，所以关掉 temperature (等效于 do_sample=False)
        with torch.no_grad():
            generated_ids = self.model.generate(
                **model_inputs,
                max_new_tokens=512,
                do_sample=False,
                eos_token_id=self.tokenizer.eos_token_id
            )

        # 4. 截取并解码大模型最新生成的回复
        generated_ids = [
            output_ids[len(input_ids):] for input_ids, output_ids in zip(model_inputs.input_ids, generated_ids)
        ]
        raw_response = self.tokenizer.batch_decode(generated_ids, skip_special_tokens=True)[0]

        # 5. 清洗并返回纯净的 SQL
        clean_sql = self._extract_sql(raw_response)
        return clean_sql

# ==========================================
# 独立测试入口 (模拟 Agent 第一次提问和第二次反思)
# ==========================================
if __name__ == "__main__":
    # 请替换为你真实的 V6 模型路径
    model_dir = "/root/autodl-tmp/qwen2.5-coder-7b-instruct"
    lora_dir = "/root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6"

    try:
        coder = CoderNode(model_dir,lora_dir)
        # --- 场景 1：第一次请求 ---
        print("\n>>> 场景 1：初始查询")
        # 这就是我们在 Agent 架构里的 Working Memory（工作记忆）！
        working_memory = [
            {
                "role": "user",
                "content": """问题：找出所有住在纽约的员工姓名。
数据库结构：
- 表 employee (emp_id [PK], name, city_id [FK->city.city_id])
- 表 city (city_id [PK], city_name)"""
            }
        ]
        sql_v1 = coder.generate(working_memory)
        print(f"🧠 大脑输出 V1:\n{sql_v1}\n")

        # --- 场景 2：沙盒报错，触发反思 (Reflexion) ---
        print(">>> 场景 2：沙盒报错，触发反思重写")
        # 我们把大模型写的错代码，以及沙盒的报错，原封不动地追加到记忆流里
        working_memory.append({"role": "assistant", "content": sql_v1})
        working_memory.append({
            "role": "user",
            "content": "执行报错：no such column: city.name。请查阅表结构，找出正确的列名并重写 SQL。"
        })
		
        sql_v2 = coder.generate(working_memory)
        print(f"🧠 大脑反思后的输出 V2:\n{sql_v2}\n")
		
    except Exception as e:
        print(f"未找到模型或加载失败，此代码仅作架构演示。报错信息：{e}")
```

### `agent_executor.py`（sql语句执行）

```python
import sqlite3
import time

class SQLSandbox:
    def __init__(self, db_path: str, timeout: int = 5):
        """
        初始化沙盒执行器。
        :param db_path: SQLite 数据库文件的物理路径
        :param timeout: 数据库锁超时时间（防止死锁）
        """
        self.db_path = db_path
        self.timeout = timeout

    def execute_query(self, sql_query: str, row_limit: int = 10) -> dict:
        """
        核心执行引擎：带有极强防御性编程的 SQL 执行器。
        :param sql_query: 大模型生成的 SQL 字符串
        :param row_limit: 限制最大返回行数，【极其关键】防止查询结果太大撑爆大模型的上下文窗口！
        """
        # 清理可能存在的代码块标记 (Markdown 格式)
        clean_sql = sql_query.replace("```sql", "").replace("```", "").strip()
        
        start_time = time.time()
        
        try:
            # 1. 建立连接（开启 URI 模式可支持只读，此处为标准模式）
            conn = sqlite3.connect(self.db_path, timeout=self.timeout)
            cursor = conn.cursor()

            # 2. 尝试执行大模型写的 SQL
            cursor.execute(clean_sql)
            
            # 3. 提取列名和数据
            # 注意：对于没有返回值的操作（如 INSERT），description 会是 None
            columns = [desc[0] for desc in cursor.description] if cursor.description else []
            
            # 【架构师细节】：绝不能用 fetchall()！如果模型写了个笛卡尔积查出10万条数据，
            # 你的 Agent 内存会当场崩盘，Token 也会直接爆炸。必须用 fetchmany()。
            results = cursor.fetchmany(row_limit) 
            
            conn.close()
            execution_time = time.time() - start_time

            # 4. 组装“成功战报”
            return {
                "status": "success",
                "sql_executed": clean_sql,
                "columns": columns,
                "results": results,
                "row_count": len(results),
                "execution_time_sec": round(execution_time, 3)
            }

        except sqlite3.Error as e:
            # 【Reflexion 的灵魂】：精准捕获数据库原生报错！
            # 这是将来喂给大模型让她自我反思的“救命稻草”。
            return {
                "status": "error",
                "sql_executed": clean_sql,
                "error_type": type(e).__name__,
                "error_msg": str(e)
            }
            
        except Exception as e:
            # 兜底防御：防止 Python 级别的其他报错搞崩整个系统
            return {
                "status": "error",
                "sql_executed": clean_sql,
                "error_type": "SystemError",
                "error_msg": str(e)
            }

# ==========================================
# 独立测试入口 (如果你直接运行这个脚本，它会执行这里的代码)
# ==========================================
if __name__ == "__main__":
    # 假设你有个 dev 数据库，替换成你真实的测试库路径
    test_db = "data/database/concert_singer/concert_singer.sqlite" 
    
    if not __import__("os").path.exists(test_db):
        print(f"⚠️ 找不到测试数据库: {test_db}，请检查路径。")
    else:
        sandbox = SQLSandbox(test_db)
        
        print(">>> 测试 1: 执行完美的 SQL")
        good_sql = "SELECT * FROM stadium LIMIT 2"
        print(sandbox.execute_query(good_sql))
        
        print("\n>>> 测试 2: 执行带语法错误的 SQL (制造 Reflexion 弹药)")
        # 故意写错表名: stadiuuuuum
        bad_sql = "SELECT name, capacity FROM stadiuuuuum"
        print(sandbox.execute_query(bad_sql))
```

- 当前的Reflexion触发条件有：
	- SQL 语法错误
	- 表名不存在
	- 列名不存在
	- 函数不存在
	- 其他 SQLite 执行异常
	- Python 级别的兜底异常

	注：”不会处理“可执行但答错了”的情况“

### 空结果重试探针（已关闭）

待补充

### 结果

#### 开启空结果重试
```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_with_empty_retry \
  --max-retries 3 \
  --retry-on-empty-result \
  --progress-every 50

```

跑完之后，再用官方评测脚本判卷：
```bash
python evaluation.py \
  --gold data/dev_gold.sql \
  --pred eval_with_empty_retry/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype all
```

结果：**75.2%**

#### 关闭空结果重试

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_no_empty_retry \
  --max-retries 3 \
  --progress-every 50
```

```bash
python evaluation.py \
  --gold data/dev_gold.sql \
  --pred eval_no_empty_retry/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype all
```

结果：**75.4%**


### ReACT阶段结束，最终没有开启空结果尝试（之后的流程是否又加回空结果尝试不清楚）


## Agentic RAG

### 版本一（仅做表筛选）

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_rag \
  --max-retries 3 \
  --schema-mode rag
```

```bash
python evaluation.py \
  --gold data/dev_gold.sql \
  --pred eval_rag/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype all

python evaluation.py \
  --gold data/dev_gold.sql \
  --pred eval_rag/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype exec \
  --dev-json data/dev.json \
  --error-report eval_rag/wrong_cases_exec.jsonl
```

- 结果：**75.6%**

### 版本二

不再只是“检索后的表结构列表”，还可以在同一个 【数据库结构】... 块里附加三类信息：

【优先关注表】
【候选连接关系】
【候选连接路径】

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_rag_path \
  --predict-file predict_agent.txt \
  --summary-file agent_run_summary.jsonl \
  --trajectory-file agent_trajectories.jsonl \
  --max-retries 3 \
  --schema-mode rag \
  --schema-path-hints
```


```bash
python evaluation.py \
  --gold data/dev_gold.sql \
  --pred eval_rag_path/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype all

python evaluation.py \
  --gold data/dev_gold.sql \
  --pred eval_rag_path/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype exec \
  --dev-json data/dev.json \
  --error-report eval_rag_path/wrong_cases_exec.jsonl
```

74.6%          

### 版本三（选择性路径提示）

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_rag_path_selective \
  --predict-file predict_agent.txt \
  --summary-file agent_run_summary.jsonl \
  --trajectory-file agent_trajectories.jsonl \
  --max-retries 3 \
  --schema-mode rag \
  --schema-path-hints-selective
```

```bash
python evaluation.py \
  --gold data/dev_gold.sql \
  --pred eval_rag_path_selective/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype all

python evaluation.py \
  --gold data/dev_gold.sql \
  --pred eval_rag_path_selective/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype exec \
  --dev-json data/dev.json \
  --error-report eval_rag_path/wrong_cases_exec.jsonl
```

75%               


## 最小化验证实验


### 数据

100道样本(100_cases.json)
从 Spider dev 里抽：

* 50 道 baseline 原本做对的
* 50 道 baseline 原本做错的


### baseline（其实不用跑测试，一定是50%）

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/100_cases.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_100_base \
  --max-retries 3 \
  --schema-mode rag
```

```bash
python evaluation.py \
  --gold data/dev_gold_100.sql \
  --pred eval_100_base/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype all
```

结果：50%               

### 候选池生成v1

由于要做oracle@5对比实验，我们首先要对每道题生成5个固定的候选sql，保存为候选池，后续的三个selector和oracle都采用该固定候选池，这样才有对比意义

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/100_cases.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_100_pool_gen \
  --max-retries 1 \
  --schema-mode rag \
  --selector-mode selector1 \
  --selector1-k 5 \
  --selector1-temperature 0.8 \
  --selector1-top-p 0.9 \
  --candidate-pool-mode generate \
  --candidate-pool-path eval_100_pool_gen/candidate_pool.jsonl


```

- 这一步的 selector-mode 只是占位，因为你真正要的是候选池本身。我这里用 selector1 占位。

- 现在候选生成逻辑是：

	- 目标每题 5 个唯一候选
	- 第一轮先采样 10 条 raw outputs
	- 去重
	- 如果还不足 5，继续补采样
	- 一直补到：
	- 凑满 5 个唯一候选，或
	- 到达最大 raw budget 30
	- 如果到 30 还不够：
	- 保留已有候选
	- 明确写 candidate_shortage=true

结果："candidate_shortage": true ----69/100

只生成了一条候选sql -----36/100

### Selector 1：Execution-first

规则：

1. 优先保留**能执行**的 SQL
2. 如果多个都能执行，优先选择：

   * 非空结果
   * 结构更完整者（可先简单用长度或 clause 数量近似）

#### 目的

验证“execution + 非空”这种最弱信号有没有用。

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/100_cases.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_100_se_1 \
  --max-retries 1 \
  --schema-mode rag \
  --selector-mode selector1 \
  --selector1-k 5 \
  --selector1-temperature 0.8 \
  --selector1-top-p 0.9 \
  --candidate-pool-mode load \
  --candidate-pool-path eval_100_pool_gen/candidate_pool.jsonl


```

```bash
python evaluation.py \
  --gold data/dev_gold_100.sql \
  --pred eval_100_se_1/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype all
```
结果：50%             

### Selector 2：Schema-validity + Execution

给每个候选打简单分：

#### 分数构成示例

* +3：能执行
* +2：结果非空
* +1：所有表列都在 schema 中
* +1：没有明显重复/畸形 SQL
* -1：明显缺失 SELECT/FROM/ORDER/GROUP 等关键结构
* -1：引用未知列
* -1：语法可执行但结构可疑（例如 group/order 不匹配，可以先做很弱的检测）

最后选分最高的。

#### 目的

验证：**纯规则质量信号**能不能比 baseline 更好。

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/100_cases.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_100_se_2 \
  --max-retries 1 \
  --schema-mode rag \
  --selector-mode selector2 \
  --selector1-k 5 \
  --selector1-temperature 0.8 \
  --selector1-top-p 0.9 \
  --candidate-pool-mode load \
  --candidate-pool-path eval_100_pool_gen/candidate_pool.jsonl


```

- 注：selector2参数设置仍然是selector1-k、selector1-temperature、selector1-top-p

```bash
python evaluation.py \
  --gold data/dev_gold_100.sql \
  --pred eval_100_se_2/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype all
```

 结果：50%               

### Selector 3：同模型判别（不训练）
#### 输入形式

给模型：

* question
* schema
* 候选 SQL 1~5

让它回答：

> 哪一个最有可能正确回答问题？只输出编号。

#### 很重要

不要让它解释太多，避免输出发散。
直接要求：

* 输出 `1/2/3/4/5`
* 或输出 JSON：`{"best": 3}`

#### 目的

验证：**同一个模型是否具备“判断优于生成”的能力**

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/100_cases.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_100_se_3 \
  --max-retries 1 \
  --schema-mode rag \
  --selector-mode selector3 \
  --selector1-k 5 \
  --selector1-temperature 0.8 \
  --selector1-top-p 0.9 \
  --candidate-pool-mode load \
  --candidate-pool-path eval_100_pool_gen/candidate_pool.jsonl


```

```bash
python evaluation.py \
  --gold data/dev_gold_100.sql \
  --pred eval_100_se_3/predict_agent.txt \
  --db data/testsuitedatabases/database \
  --table data/tables.json \
  --etype all
```

结果： 59%               
### 计算Oracle@5

```bash
python oracle_at_k.py \
  --candidate-pool-path eval_100_pool_gen/candidate_pool.jsonl \
  --dev-path data/100_cases.json \
  --db-root data/testsuitedatabases/database \
  --attempt-scope first \
  --output-path eval_100_pool_gen/oracle_at_5.json

```

{
  "total_count": 100,
  "oracle_success_count": 65,
  "oracle_accuracy": 0.65,
  "attempt_scope": "first"
}

### 最终实验结果：放弃候选池路线

## 路线探索

### phase 1

`phase1` 的非训练版 router 已经接进代码了，核心思路就是：

- 默认还是 `baseline`
- 先经过 `phase0` 的两层排除边界
- 对剩余可模板化题目，先生成候选模板集合
- 再让当前基座模型输出一个 JSON：
  - `use_template_score`
  - `selected_template`
  - `template_scores`
  - `reason`
- 只有当分数超过阈值时，才允许模板路线接管；否则直接回退 baseline

这次主要改了三处：

- [superlative_solver.py](D:/VisSQL-Agent/superlative_solver.py)
  - 新增 `phase1` 模式
  - 新增非训练版 router prompt 与 `_route_templates(...)`
  - 新增 `_router_accepts(...)`
  - 新增 `uses_phase1_router(...)`
  - 新增 `get_candidate_templates(...)`
  - `phase1` 会复用 `phase0` 的排除层，所以它不是裸 LLM 判别，而是“排除层 + LLM router + 阈值”
- [main_agent.py](D:/VisSQL-Agent/main_agent.py)
  - 透传了两个 router 阈值
  - 保留了 `pattern_result`
  - 在模板命中的记录里新增了 `candidate_templates` 和 `router_decision`
  - generic fallback 路径现在也会把 `pattern_result` 带出去，方便你分析为什么没接管
- [main_eval_agent.py](D:/VisSQL-Agent/main_eval_agent.py)
  - 新增 `--superlative-mode phase1`
  - 新增两个参数：
    - `--superlative-router-use-threshold`
    - `--superlative-router-template-threshold`
  - summary 里新增了：
    - `route`
    - `pattern_reason`
    - `pattern_template`
    - `pattern_candidate_templates`
    - `pattern_router_decision`

默认阈值我先设成了偏保守的：
- `use_template_score >= 0.70`
- `selected_template_score >= 0.65`

也就是说，`phase1` 现在是一个“可放弃”的 router，不会因为一点点模板相似性就强行接管。

我已经做过语法检查，`python -m py_compile superlative_solver.py main_agent.py main_eval_agent.py` 通过了，但还没有替你跑完整评测。

你现在可以直接这样跑 `phase1`：

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_phase1 \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1 \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65
```

如果你愿意，我下一步可以继续帮你做一件很关键的事：先不跑全量，先构造一个 `phase1` 的小规模 sanity-check，专门检查 router 是否真的只在高置信题上接管，以及它的 `pattern_router_decision` 日志长什么样。

#### 结果

75.4%

### phase1 b

已把这两件事落到代码里了，核心改动都在 [superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:41)，文档也同步更新到了 [myagent_v2.md](/D:/VisSQL-Agent/myagent_v2.md:149)。

这次新增的是 `Template E = JOIN_GROUP_COUNT_TOP1`。它现在已经完整接入了候选模板生成、phase1 router 的候选与打分信号、slot schema、SQL builder、template validator，入口和主链路分别能在 [superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:128)、[superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:652)、[superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:763)、[superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:977)、[superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:1110)、[superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:1275) 看到。它解决的是“最终输出在实体表，但计数发生在关联 fact 表”的那类题。

`GROUP_COUNT_TOP1` 也做了保守强化。现在我补了更完整的计数型表述词和软模式，避免像 `highest number of`、`largest number of`、`most frequent` 这种明显属于计数最高级的问法漏掉；同时把模板边界收紧了：`GROUP_COUNT_TOP1` 的 `target` 和 `group_key` 必须落在同一分组侧，`join_clause` 只能做过滤/上下文，不能再偷偷承担“把答案切到另一张实体表”的职责。这部分主要在 [superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:41)、[superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:900)、[superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:1228)。另外我还加了 join predicate 校验，默认拒绝 `OR` 型连接，并要求 `JOIN_GROUP_COUNT_TOP1` 至少存在直接 FK 邻接，这样能把一些高风险错配挡掉，位置在 [superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:396) 和 [superlative_solver.py](/D:/VisSQL-Agent/superlative_solver.py:1182)。

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_phase1_b \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1 \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65
```

#### 结果
 75.0%
### phase 1 c

已实现 phase1_c，它不会覆盖原来的 phase1 行为。入口参数已加到 main_eval_agent.py (line 67)，核心逻辑在 superlative_solver.py (line 742)。

这版主要做了三件事：slot prompt 在 phase1_c 下会给出 schema column candidates，并要求 LLM 返回 target_columns；接管前会做 projection completeness check，发现“问题要求多列但 slot 只有一列”时只修复 target slot；另外只在 phase1_c 下新增两个安全回退边界，挡住排序列表题和 per-group percentage extrema 题。

已验证：python -m py_compile superlative_solver.py main_eval_agent.py main_agent.py 通过。

运行命令：

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_phase1_c \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1_c \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65

```

#### 结果

74.5%

### phase 1 d

已做出 phase1_d。

核心变化在 superlative_solver.py (line 125)：

新增模板 ENTITY_BY_RELATED_COUNT_TOP1。
从 SQLite schema 外键图枚举 entity_table <- fact_table 的 count plan。
LLM 不再自由写 join_on/entity_table/fact_table，只选择枚举出来的 plan_id、target_columns、group_by_column、condition。
phase1_d 不启用 phase1_c 的 controlled slot prompt / LLM repair。
phase1_d 只保留 projection completeness validator，发现投影不完整则 fallback baseline。
已把 --superlative-mode phase1_d 加到 main_eval_agent.py (line 67)。
已把 phase1_c 反思和 phase1_d 设计补进 myagent_v2.md (line 962)。

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_phase1_d \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1_d \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65

```

#### 结果

75.7%

### phase 1 e

phase1_e 已经实现好了，核心改动在 superlative_solver.py (line 690) 和 main_eval_agent.py (line 67)，文档也补到了 myagent_v2.md (line 999)。

这版做的是保守版的“structured projection selector”：

只作用于 GROUP_COUNT_TOP1 / JOIN_GROUP_COUNT_TOP1 / ENTITY_BY_RELATED_COUNT_TOP1
先保留 phase1_d 的结构槽位，再由程序枚举 answer-side 的可投影列，让 LLM 只选 target_columns
不做 phase1_c 那种 repair
仍然保留 projection_incomplete -> fallback baseline
对 GROUP_COUNT_TOP1 额外做了一个很小的对齐：如果当前其实是在 join 出来的 entity 侧取答案，会把 group_key 同步到 answer-side，避免“列补全了但 validator 还是挡掉”


```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_phase1_e \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1_e \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65

```

#### 结果

75.7%

### phase 2 a

phase2_a 已经加进代码了，而且这次只做了你刚才确认过的那件事：统一的 count-family planner，没有加 validator-aware final score。核心入口在 superlative_solver.py (line 1036)、builder/validator 在 superlative_solver.py (line 1952) 和 superlative_solver.py (line 2150)，模式开关在 main_eval_agent.py (line 67)，设计说明补到了 myagent_v2.md (line 1029)。

这版新增了统一模板 COUNT_FAMILY_TOP1。它会先从 schema 枚举两类 plan：same_table 和 related_entity，再让 LLM 只选 plan_id / target_columns / group_by_column / order / condition / include_count。也就是说，count-family 不再靠三套松散模板各自自由填槽，而是走统一 planner；旧的 GROUP_COUNT_TOP1 / JOIN_GROUP_COUNT_TOP1 / ENTITY_BY_RELATED_COUNT_TOP1 还保留给 phase1_*，不会被覆盖。

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_phase2_a \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase2_a \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65
```

#### 结果

75.4%

### phase 2 b

phase2_b 已经做好了，重点就是你刚才要的那条：只针对 COUNT_FAMILY_TOP1 提高覆盖率，核心手段是降低 slot_extraction_failed。

这版的关键变化都在 superlative_solver.py (line 1066) 开始的这几段：

superlative_solver.py (line 1066)
先按 slot_hint 更 aggressively 地裁剪 count-family plans
superlative_solver.py (line 1127)
先单独选 plan_id
superlative_solver.py (line 1177)
再单独选 target_columns
superlative_solver.py (line 1246)
group_by_column 改成程序推断
superlative_solver.py (line 1267)
condition 单独抽，失败就留空
superlative_solver.py (line 1311)
COUNT_FAMILY_TOP1 的完整新抽槽主链路
另外我顺手做了两件泛化增强：

phase2_b 允许 count-output 子类继续走 COUNT_FAMILY_TOP1
入口在 superlative_solver.py (line 1974) 和 superlative_solver.py (line 1994)
per-group percentage extrema 这类明显不属于 count-family 的题，现在会继续被挡掉
位置在 superlative_solver.py (line 2006)
命令行入口也加好了，在 main_eval_agent.py (line 67)。

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_phase2_b \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase2_b \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65
```

#### 结果

忘了

### 路线转移至 p0

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_p0 \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1_e \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65

```

#### 结果

78.5%

### p1

这一步已经接好了，当前是“程序判定的最小版 verifier”。

改动在：

semantic_verifier.py
agent_memory.py
main_agent.py
main_eval_agent.py
现在的行为是：

generic LLM 生成 SQL
SQL 先执行
如果能执行，SemanticVerifier 会检查 3 类风险
entity_filter_mismatch
group_projection_mismatch
setop_semantics_risk
只有当风险较高时，才会把修复提示写回 memory，再重试一次
不是高风险就直接保留原结果，避免 verifier 过度干预
我还把结果写进了 summary：

semantic_retry_count
final_verifier_result
已经做过的验证：

python -m py_compile semantic_verifier.py agent_memory.py main_agent.py main_eval_agent.py
几条针对性 smoke test，确认它能抓到：
flights.Airline = "United Airlines" 这种 FK/name 混用
top-1 entity 题里多投影 COUNT(*)
INTERSECT COUNT(*) 这种集合语义错位

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_p1 \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1_e \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65

```

#### 结果

77.3%

### p1.1

这版 P1.1 已经改好了，重点是两件事：

在 semantic_verifier.py (line 294) 去掉了最伤分的 missing_group_by 高风险规则，不再因为题目里出现 each 就强行要求 GROUP BY。
在 main_agent.py (line 67) 到 main_agent.py (line 345) 给 generic 路径加了保底逻辑：
保留第一条执行成功的 SQL
如果 semantic retry 进入“同样高风险 + 同样 SQL”的循环，就直接回退
如果后续重试最后仍高风险、变空结果，或直接报错，也回退到第一条成功 SQL
另外我把 fallback 信息接进了 main_eval_agent.py (line 266) 和 main_eval_agent.py (line 320)，你跑完后可以直接看：

used_success_fallback
success_fallback_reason
selected_success_attempt

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_p1_1 \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1_e \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65
```

#### 结果

78.9%

### column_retriever

列级检索已经接进主流程了，核心改动在 schema_retriever.py (line 853) 和 main_eval_agent.py (line 230)。

现在的 SchemaRetriever 会先做原来的表级打分，再额外做一层列级打分，把高分列回流成表分数增益；同时会把命中的列以 【优先关注字段】 的形式写进 schema prompt。评测日志里也新增了这些字段，方便你后面分析：

schema_column_hints_enabled
schema_column_hint_columns
schema_table_scores_lexical
schema_table_column_boosts
schema_column_scores
我做过两类验证：

python -m py_compile D:\VisSQL-Agent\schema_retriever.py D:\VisSQL-Agent\main_eval_agent.py
smoke test 确认已经能产出像 airports.AirportCode、country.Region、car_makers.FullName 这类列命中，并且这些命中会影响 seed table 排序
这版是“轻量列级检索”，还没有上 QueryWeaver 那种更强的“列级检索 + bridge table shortest path completion”全套。下一步最自然的动作就是直接跑一轮评测，看这版单独能不能继续把 where/select/group 这几类 generic_llm 错题往下压。

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_column \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1_e \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65

```

#### 结果

77.5%

### column_v2

v2 保守版 已经改好了，核心都在 schema_retriever.py (line 1167) 这块。

这版做了三件事：

给列打分加了歧义抑制和泛 id/code/fk 降权
把列分对表分的影响从强融合改成了小幅 boost
把 【优先关注字段】 改成高置信才展示，现在很多噪声题会直接不展示列提示
我已经做过两类检查：

python -m py_compile D:\VisSQL-Agent\schema_retriever.py D:\VisSQL-Agent\main_eval_agent.py
spot check 确认像 dog_kennels、car_1 这类高歧义场景里，列提示已经明显变稀疏了；dog_id / treatment_type_code / ModelId 这类泛列不会再轻易进 prompt

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_column_v2 \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1_e \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65
```

#### 结果
79.1%


### Selective Bridge Completion v1

Selective Bridge Completion 已经接进主链路了，改动主要在 schema_retriever.py (line 1057) 和 main_eval_agent.py (line 246)。

这版做的是一个保守实现：

先用现有 seed_tables + 高置信列命中 选出少量 anchor tables
只在 FK 图上做 shortest-path bridge completion
只补真正缺失的中间桥表
不额外给模型增加新的桥接提示文本，先只通过“把桥表放进检索子图”来生效
我已经做过两类检查：

python -m py_compile D:\VisSQL-Agent\schema_retriever.py D:\VisSQL-Agent\main_eval_agent.py
spot check，确认像 student_transcripts_tracking 这种题现在会自动补出 Student_Enrolment 作为桥表；而 museum_visit / flight_2 这种本来不需要桥表的题不会乱补
评测日志也接好了，你跑完后可以直接看这些字段：

schema_bridge_completion_enabled
schema_bridge_anchor_tables
schema_bridge_paths
schema_bridge_added_tables

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_bridge_v1 \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1_e \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65

```

#### 结果

79.4%

### seed-first-only（关闭bridge消融）

已经做好了，bridge_v1 代码保留不动，同时新增了一个评测开关：

main_eval_agent.py (line 97) 新增 --disable-bridge-completion
默认还是现在的 bridge_v1 行为
加上这个参数后，会关闭显式 shortest-path bridge completion，但保留 seed-first 的 selected_tables 保留顺序

```bash
python main_eval_agent.py \
  --base-model /root/autodl-tmp/qwen2.5-coder-7b-instruct \
  --lora-path /root/autodl-tmp/LLaMA-Factory/saves/Qwen2.5-7B/lora/qwen_spider_lora_v6 \
  --dev-path data/dev.json \
  --tables-path data/tables.json \
  --db-root data/testsuitedatabases/database \
  --output-dir eval_seed_first_only \
  --max-retries 3 \
  --schema-mode rag \
  --superlative-mode phase1_e \
  --superlative-router-use-threshold 0.70 \
  --superlative-router-template-threshold 0.65 \
  --disable-bridge-completion

```

#### 结果

79.4%

## explanation

纯 explanation 版已经接好了，而且是零干预实现。

改动在：

schema_retriever.py (line 1057)
main_eval_agent.py (line 246)
现在 retrieval_info 里会新增：

retrieval_explanation.confidence
retrieval_explanation.table_rationales
retrieval_explanation.dropped_candidates
retrieval_explanation.ambiguities
同时我也把它写进了摘要日志字段：

schema_retrieval_explanation
这版不会改：

seed_tables
selected_tables
schema_text
prompt
agent 决策
所以它只是解释层，不会主动影响当前基线准确率。

#### 结果

79.4%



## 至此，整个项目完结，最终版为explanation 79.4%



